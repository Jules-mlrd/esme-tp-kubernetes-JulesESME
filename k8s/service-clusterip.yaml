apiVersion: v1
kind: Service
metadata:
  name: esme-app-clusterip
  namespace: esme-tp-julesmlrd
  labels:
    app: esme-devops-app
    service-type: internal
spec:
  type: ClusterIP
  selector:
    app: esme-devops-app
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
  sessionAffinity: None

